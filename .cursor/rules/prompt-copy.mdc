---
description: 效果 prompt.md 的文案规则——每个事实只出现一次，效果描述只写「看到什么、怎么动、我操作它怎样」
globs: effects/**/prompt.md
alwaysApply: false
---

# 效果描述文案规则

## 第一性原理

prompt 的读者是 AI 编程助手，发送者是不懂开发的用户。引擎把 prompt 拼成固定几段，**每个事实只在最合适的一段出现一次**：

| 段 | 来源 | 只放什么 |
| --- | --- | --- |
| 【任务】 | meta.json `name` + `summary` | 名字和一句话摘要 |
| 【效果描述】 | `## 效果描述` | 眼睛看到什么、动起来什么样、我操作它怎么反应 |
| 【参数】 | meta.json `params`（引擎自动列出，每行带参考实现里的落点 `--mt-<key>` / `CONFIG.<key>` 与 `help`） | 所有数值、颜色、路径、张数、开关 |
| 【技术要求】 | 全局四条 + `## 技术要求补充` | 拖拽 / 键盘 / 暂停 / 无障碍 / reduced-motion 等约定 |
| 【实现提示】 | `## 实现提示`（只在「附加代码」关闭时输出） | 给 AI 的技术路线，可用术语 |
| 【完成后请检查】 | `## 完成后请检查` + 引擎追加的「逐项核对参数已写入」 | 只写照着描述写也容易做错的点 |
| 【参考实现】 | 导出代码（开关打开时内联）；关闭时给出静态端点 `code/<slug>.html` 与 `meta/<slug>.json` 的抓取地址 | 终极规格，数值已写入；【任务】末尾声明取舍顺序：参考实现 > 参数 > 描述 |
| 在线预览 | 站内复制时附带（带当前参数的 hash 路由地址） | 给人核对用，明确写「AI 无需访问」 |

## `## 效果描述` 怎么写

1. 三到四句，顺序固定：**静止时的样子 → 动起来怎样 → 我操作时怎样反应 →（可选）边界**（循环 / 到头 / 不操作时）。全文 ≤ 200 字。
2. **不写任何数值**（数字、颜色值、路径、张数、秒数）：值全部在【参数】。要指代参数时用它的**角色词**，与 meta.json 的 `label` 同词：「强调色」「切换时长」「每张停留」「感应半径」。
3. **不重复摘要**：第一句直接进入画面，不要「一个 XX 效果：」开头。
4. **不写实现手段**（transform / perspective / clip-path / canvas / IntersectionObserver…）：放进 `## 实现提示`。
5. **不写交互约定**（键盘、拖拽手势阈值、悬停暂停、aria）：放进 `## 技术要求补充`。用户能感知的核心操作（"拖住顶卡往左右甩"）可以写。
6. 词汇面向非开发者：图片 / 卡片 / 小圆点 / 箭头 / 进度条 / 标题条。比喻最多一处，放在最后一句。
7. 不用 `{{key}}` 占位符（validate 会拦）。

## `## 完成后请检查`

2–3 条，只写容易做错的：接缝是否无缝、到头怎么办、暂停 / 恢复、原有内容是否受影响。不重述外观。

## `## 实现提示`

给 AI 的技术路线，只在「附加代码」关闭时出现（默认关闭）。此时 prompt 里同时给了 `code/<slug>.html` 的抓取地址，能联网的 AI 会先取回参考实现；实现提示是**取不到时的唯一规格**，所以必须能独立支撑还原：

1. **技术路线**一两句：结构（DOM / canvas / SVG）、关键 CSS 属性或算法、易错点（如「别用锚点，会把整页滚到顶」）。
2. **所有决定观感但没有参数化的常量都要写出来**：尺寸与比例、基准时长与相位错开方式、缓动、透明度、混色模式、投影、层数、阈值、非参数颜色。还原度实测的结论：离线 AI 的还原度与这些常量的密度成正比——球池的提示把每个数字都写了，重写结果几乎一致；极光的提示只有一句话，光斑尺寸 / 透明度 / 周期全靠猜，观感走样。
3. **引用参数一律用 `{{key}}`**（引擎渲染时替换成当前值），不要写参数的默认数值——用户调参后写死的数字会与【参数】冲突。`pnpm validate` 会对写死默认值的提示告警。
4. 不写站点内部机制（thumb 演示块、`__MT_ENV`、runtime、postMessage）与 `/samples/` 路径。
5. 篇幅 300–700 字，`- ` 列表或短段落均可。

## meta.json `help`

会跟在【参数】的值后面。凡是「光看数字不知道什么意思」的参数都要写：`磁力强度：0.35（按钮被吸走的幅度占鼠标偏移的比例）`。

## 例子

```markdown
<!-- ❌ 值散落在描述里、三处重复、夹带实现手段 -->
一个文字为 {{label}} 的按钮（底色 {{color}}、圆角 {{rounded}}）。鼠标进入约 {{radius}} 的感应范围时……
偏移幅度是距离的 {{strength}} 倍（transform: translate 实现）……

<!-- ✅ 只写体验，用角色词指代参数 -->
一枚普通的按钮静静待在原位。鼠标进入它周围的感应半径后，按钮像被磁铁吸住一样朝鼠标方向轻轻偏移，
离鼠标越近偏得越多（幅度由磁力强度决定）；鼠标离开后，按钮用一条带轻微过冲的弹簧曲线弹回原位。
整体手感是「按钮想跟着你的鼠标走」，俏皮但不夸张。

## 实现提示
pointermove 时算鼠标到按钮中心的向量，乘以磁力强度写入 transform: translate；离开时切到带过冲的 cubic-bezier 过渡回零。
```
