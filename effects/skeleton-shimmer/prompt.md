## 效果描述
一组内容占位骨架：左上一个圆形头像位，右侧一条标题条和 {{rows}} 行文字条（最后一行短一截），下方一块大图占位。所有骨架块底色为 {{base}}、圆角 {{rounded}}，表面有一道 {{highlight}} 颜色的斜向高光带，以 {{speed}} 的速度从左到右周期性扫过（linear-gradient 背景 + background-position 位移），传达「内容正在加载」。扫光在所有块上同步进行，节奏统一。

## 完成后请检查
- 骨架布局与描述一致（头像 + 标题 + {{rows}} 行文字 + 图块），最后一行明显更短
- 扫光方向从左到右、循环无跳变，颜色与速度符合参数
- 容器带 role="status" 与 aria-label；系统开启「减少动态效果」时扫光停止、保留静态骨架

## 放在哪
数据到达前的列表项、卡片、文章详情等位置；骨架的形状请按你页面的真实内容结构改造，加载完成后整块替换为真实内容
